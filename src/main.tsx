import React from 'react'
import ReactDOM from 'react-dom/client'
import {PortableTextOfLife} from './portable-text-of-life.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PortableTextOfLife />
  </React.StrictMode>,
)
